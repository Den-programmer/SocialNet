import React from 'react';
import classes from './btn_showMore.module.css';

const BtnShowMore = (props) => {
    return (
        <div className={classes.btn_showMore}>
            <button>
                Show More
            </button>
        </div>
    );
}

export default BtnShowMore;